{% extends 'layout' %}
{% block main %}
<body>
  <main class="bd-masthead" id="content" role="main">
    <div class="container">
      <div class="row">
        <div class="col-1 col-sm-0 col-md-0"></div>
        <div class="col-lg-10 col-sm-12 col-md-12">
          {% for entry in craft.entries.section('secestdesign') %}
          {% for block in entry.matriz %}
          {{ block.body }}
          {% endfor %}
          {% endfor %}
        </div>
        <div class="col-1 col-sm-0 col-md-0"></div>
      </div>
    </div>
  </main>
</body>
{% endblock %}
