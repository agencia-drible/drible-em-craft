{% extends 'layout' %}
{% block main %}
<main class="bd-masthead" id="content" role="main">
  <div class="container">
    <div class="row">
      <div class="col-1"></div>
      <div class="col-10">
      {% for entry in craft.entries.section('webscripts') %}
      {% for block in entry.adicionar %}
      {{ block.conteudo }}
      {% endfor %}
      {% endfor %}
    </div>
    <div class="col-1"></div>
    </div>
  </div>
</main>
{% endblock %}
