{% extends 'layout' %}
{% block main %}
<main class="bd-masthead" id="content" role="main">
  <div class="container">
    <div class="list-group">
      <!-- <div class="listatwig">
         ESCREVER PARA A LISTA
        <h5 class="pt-3"> Lista de conteudo: </h5>

         {% for entry in craft.entries.section('webprocessos') %}
        {% for entries in entry %}

        <p class="text-dark"><a href="processos#{{entry.slug}}"> {{entry.slug}}</a></p>

        {% endfor %}
        {% endfor %}
      </div> -->
      <!-- ========================================================== -->
      <br>

      <!-- {% for entry in craft.entries.section('webprocessos') %}
      {% for entries in entry %}

      <div id="{{entry.slug}}">

      {% endfor %}
      {% endfor %} -->

      {% for entry in craft.entries.section('webprocessos') %}
      {% for block in entry.adicionar %}
      <div class="quadro">
        {{ block.conteudo }}
      </div>
      {% endfor %}
      {% endfor %}

    </div>
  </div>
</div>
</main>
{% endblock %}
