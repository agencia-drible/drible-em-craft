{% extends 'layout' %}
{% block main %}
<body>
  <main class="bd-masthead" id="content" role="main">
    <div class="container">

      {% for entry in craft.entries.section('secinit') %}
      {% for block in entry.matriz %}
      {{ block.body }}
      {% endfor %}
      {% endfor %}

    </div>
  </main>
</body>
{% endblock %}
