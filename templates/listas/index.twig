{% extends 'layout' %}
{% block main %}
<div class="container">
  <div class="row">
    <div class="col-lg-10 col-sm-12 col-md-12 mx-auto">
      <ul>
        {% set es = craft.entries().section(entry.slug) %}
        {% for e in es %}
        <li><p><a href="/{{ entry.title }}/{{ e.slug }}" class="ank-link">{{ e.title }}</a></p></li>
        {% endfor %}
      </ul>
    </Idiv>
  </div>
</div>
{% endblock %}
